/** Login POST request timeout (`AbortSignal`). */
export const LOGIN_SUBMIT_TIMEOUT_MS = 25_000

/** `GET /admin/me` guard request timeout (`RequireAdminSession`). */
export const ADMIN_ME_GUARD_TIMEOUT_MS = 20_000
